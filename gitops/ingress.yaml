# 3. Standard Ingress
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: traefik-ingress
  namespace: traefik-ingress-demo
  annotations:
    kubernetes.io/ingress.class: "traefik"
    traefik.ingress.kubernetes.io/router.tls.options: "namespace: traefik-ingress-demo-traefik-mtls-policy@kubernetescrd"
    traefik.ingress.kubernetes.io/router.middlewares: "namespace: traefik-ingress-demo-pass-client-cert@kubernetescrd"
spec:
  ingressClassName: traefik-ingress
  tls:
  - hosts:
    - traefik.34.42.98.217.nip.io
    secretName: traefik-server-certs
  rules:
  - host: traefik.34.42.98.217.nip.io
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: traefik-service
            port:
              number: 80