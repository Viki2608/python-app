apiVersion: apps/v1
kind: Deployment
metadata:
  name: python-traefik-demo
  namespace: traefik-ingress-demo
spec:
  replicas: 1
  selector:
    matchLabels:
      app: python-traefik-demo
  template:
    metadata:
      labels:
        app: python-traefik-demo
    spec:
      containers:
      - name: poc
        image: us-central1-docker.pkg.dev/playground-s-11-31f44a5d/my-repo/poc:v1     
        ports:
        - containerPort: 8000
---
apiVersion: v1
kind: Service
metadata:
  name: traefik-service
  namespace: traefik-ingress-demo
spec:
  type: ClusterIP
  selector:
    app: python-traefik-demo
  ports:
  - port: 80
    targetPort: 8000